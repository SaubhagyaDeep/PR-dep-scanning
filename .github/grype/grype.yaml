# Grype configuration
# https://oss.anchore.com/docs/reference/grype/configuration/


output: json
sort-by: severity
add-cpes-if-none: true
only-fixed: true
fail-on-severity: high

log:
  quiet: true

db:
  max-allowed-built-age: 72h

# CVEs with no patch are excluded — there is nothing actionable.


ignore:
  # ── Accepted exceptions ────────────────────────────────────────────────────
  # Format:
  #   - vulnerability: GHSA-xxX-xXxx  OR
  #   - package:
  #       name: package-name
  #     reason: "one-line justification"
  #     expires: "YYYY-MM-DD"     # no built-in enforcement — reviewed quarterly
  #     approved-by: "@slack-handle"

  ##- vulnerability: GHSA-35jh-r3h4-6jhm
  - package:
      name: lodash
    reason: "_.template not used in any production code path, risk accepted"
    expires: "2026-10-01"
    approved-by: "@saubhagya"

  - package:
      name: path-to-regexp
    reason: "testing it"
    expires: "2026-10-01"
    approved-by: "@saubhagya"
    
  # ── Structural ignores (no approval needed) ───────────────────────────────

  # wont-fix: upstream has explicitly declined to patch — nothing actionable
  - fix-state: "wont-fix"

  # Dev/test-only package types that never ship to production
  # - package:
  #     type: java-archive
  #     location: "**/test/**"

